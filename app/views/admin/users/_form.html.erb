<div class = "row">
  <div class = "col-md-4">
    <%= simple_form_for ([:admin, user]) do |f| %>
        <%= f.input :email %>
        <%= f.input :password %>
        <%= f.input :admin, label: "is an Admin?" %>
        <%= f.button :submit, class: "btn-primary" %>
    <% end %>
  </div>
</div>
